package edu.academic.integrity.controller;

/** Top-level alias for clients that do not use the controller's nested callback name. */
public interface TaskCallback<T> extends ApplicationController.TaskCallback<T> { }
